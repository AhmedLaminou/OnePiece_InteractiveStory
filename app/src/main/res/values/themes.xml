<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme - Premium Dark Theme -->
    <style name="Base.Theme.OnePieceInteractiveStory" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/op_red_primary</item>
        <item name="colorSecondary">@color/op_gold_primary</item>
        <item name="android:statusBarColor">@color/bg_primary</item>
        <item name="android:navigationBarColor">@color/bg_primary</item>
        <item name="android:windowBackground">@color/bg_primary</item>
    </style>

    <style name="Theme.OnePieceInteractiveStory" parent="Base.Theme.OnePieceInteractiveStory" />

    <!-- Splash Screen Theme - Updated for custom Background -->
    <style name="Theme.OnePieceInteractiveStory.Splash"
        parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_background</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <!-- Light Theme (for toggle) -->
    <style name="Theme.OnePieceInteractiveStory.Light" parent="Theme.Material3.Light.NoActionBar">
        <item name="colorPrimary">@color/op_red_primary</item>
        <item name="colorSecondary">@color/op_gold_primary</item>
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:navigationBarColor">@color/white</item>
        <item name="android:windowBackground">@color/white</item>
    </style>
</resources>