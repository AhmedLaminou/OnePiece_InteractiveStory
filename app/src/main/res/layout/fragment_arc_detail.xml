<?xml version="1.0" encoding="utf-8"?>
<androidx.core.widget.NestedScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#1A1A1A">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingBottom="32dp">

        <androidx.viewpager2.widget.ViewPager2
            android:id="@+id/slides_pager"
            android:layout_width="match_parent"
            android:layout_height="250dp"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/arc_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Arc Title"
            android:textColor="#FFD700"
            android:textSize="28sp"
            android:textStyle="bold"
            android:layout_margin="16dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/slides_pager" />

        <!-- Arc Info Badges -->
        <LinearLayout
            android:id="@+id/arc_info_badges"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="16dp"
            android:layout_marginTop="8dp"
            android:orientation="horizontal"
            app:layout_constraintTop_toBottomOf="@id/arc_title">

            <!-- Saga Badge -->
            <TextView
                android:id="@+id/saga_badge"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:background="@drawable/bg_badge_saga"
                android:paddingHorizontal="12dp"
                android:paddingVertical="4dp"
                android:text="East Blue Saga"
                android:textColor="#FFFFFF"
                android:textSize="12sp"
                android:textStyle="bold" />

            <!-- Chapter Count Badge -->
            <TextView
                android:id="@+id/chapter_count_badge"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="8dp"
                android:background="@drawable/bg_badge_chapters"
                android:paddingHorizontal="12dp"
                android:paddingVertical="4dp"
                android:text="49 Chapters"
                android:textColor="#FFFFFF"
                android:textSize="12sp"
                android:textStyle="bold" />

            <!-- Character Count Badge -->
            <TextView
                android:id="@+id/character_count_badge"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="8dp"
                android:background="@drawable/bg_badge_characters"
                android:paddingHorizontal="12dp"
                android:paddingVertical="4dp"
                android:text="12 Characters"
                android:textColor="#FFFFFF"
                android:textSize="12sp"
                android:textStyle="bold" />

        </LinearLayout>

        <!-- Chapter Range -->
        <TextView
            android:id="@+id/chapter_range"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="16dp"
            android:layout_marginTop="8dp"
            android:text="Chapters 909 - 1057"
            android:textColor="#AAAAAA"
            android:textSize="13sp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/arc_info_badges" />

        <TextView
            android:id="@+id/arc_summary"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Summary..."
            android:textColor="#FFFFFF"
            android:layout_marginHorizontal="16dp"
            android:layout_marginTop="12dp"
            app:layout_constraintTop_toBottomOf="@id/chapter_range" />

        <!-- YouTube Player Section -->
        <TextView
            android:id="@+id/video_header"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Iconic Moments"
            android:textColor="#FFD700"
            android:textSize="20sp"
            android:layout_margin="16dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/arc_summary" />

        <com.pierfrancescosoffritti.androidyoutubeplayer.core.player.views.YouTubePlayerView
            android:id="@+id/youtube_player_view"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="16dp"
            app:layout_constraintTop_toBottomOf="@id/video_header"
            app:autoPlay="false"
            app:enableAutomaticInitialization="false" />

        <!-- Characters Section -->
        <TextView
            android:id="@+id/chars_header"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif-condensed-medium"
            android:paddingHorizontal="16dp"
            android:paddingTop="24dp"
            android:paddingBottom="8dp"
            android:text="KEY CHARACTERS"
            android:textColor="@color/op_gold"
            android:textSize="18sp"
            android:letterSpacing="0.1"
            app:layout_constraintTop_toBottomOf="@id/youtube_player_view" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/charactersRecycler"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:clipToPadding="false"
            android:orientation="horizontal"
            android:paddingHorizontal="12dp"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            app:layout_constraintTop_toBottomOf="@id/chars_header" />

        <!-- Chapters Section -->
        <TextView
            android:id="@+id/chapters_header"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif-condensed-medium"
            android:paddingHorizontal="16dp"
            android:paddingTop="24dp"
            android:paddingBottom="8dp"
            android:text="CHAPTERS"
            android:textColor="@color/op_gold"
            android:textSize="18sp"
            android:letterSpacing="0.1"
            app:layout_constraintTop_toBottomOf="@id/charactersRecycler" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/chaptersRecycler"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:clipToPadding="false"
            android:orientation="vertical"
            android:nestedScrollingEnabled="false"
            android:paddingHorizontal="16dp"
            android:paddingBottom="24dp"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            app:layout_constraintTop_toBottomOf="@id/chapters_header" />

        <Button
            android:id="@+id/btn_start_quiz"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Start Quiz"
            android:backgroundTint="#D32F2F"
            android:textColor="#FFFFFF"
            android:layout_margin="16dp"
            app:layout_constraintTop_toBottomOf="@id/characters_recycler" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</androidx.core.widget.NestedScrollView>