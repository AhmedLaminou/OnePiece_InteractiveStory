<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/black">

    <!-- Background Image -->
    <ImageView
        android:id="@+id/feedBackgroundImage"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:contentDescription="@string/app_name"
        tools:src="@drawable/placeholder_character" />

    <!-- Gradient Overlay for text readability -->
    <View
        android:id="@+id/gradientOverlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/feed_gradient_overlay" />

    <!-- Content Container -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:orientation="vertical"
        android:padding="24dp"
        android:paddingBottom="100dp">

        <!-- Type Chip -->
        <TextView
            android:id="@+id/feedTypeChip"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/chip_background"
            android:paddingHorizontal="12dp"
            android:paddingVertical="6dp"
            android:textColor="@color/white"
            android:textSize="12sp"
            android:textStyle="bold"
            tools:text="⚔️ CHARACTER" />

        <!-- Title -->
        <TextView
            android:id="@+id/feedTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:textColor="@color/white"
            android:textSize="32sp"
            android:textStyle="bold"
            android:fontFamily="sans-serif-black"
            android:shadowColor="#80000000"
            android:shadowDx="2"
            android:shadowDy="2"
            android:shadowRadius="4"
            tools:text="Monkey D. Luffy" />

        <!-- Subtitle -->
        <TextView
            android:id="@+id/feedSubtitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:textColor="#CCFFFFFF"
            android:textSize="16sp"
            android:textStyle="italic"
            tools:text="Captain of the Straw Hat Pirates" />

        <!-- Stats -->
        <TextView
            android:id="@+id/feedStats"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:textColor="#FFD700"
            android:textSize="18sp"
            android:textStyle="bold"
            android:visibility="gone"
            tools:text="💰 ₿3,000,000,000"
            tools:visibility="visible" />

        <!-- Description -->
        <TextView
            android:id="@+id/feedDescription"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:textColor="#E0FFFFFF"
            android:textSize="14sp"
            android:lineSpacingMultiplier="1.3"
            android:maxLines="4"
            android:ellipsize="end"
            tools:text="The founder and captain of the Straw Hat Pirates. He ate the Gomu Gomu no Mi, becoming a rubber man." />

        <!-- Action Button -->
        <TextView
            android:id="@+id/btnAction"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:background="@drawable/btn_action_gradient"
            android:gravity="center"
            android:paddingVertical="14dp"
            android:text="View Details"
            android:textColor="@color/white"
            android:textSize="16sp"
            android:textStyle="bold"
            android:visibility="gone"
            tools:visibility="visible" />

    </LinearLayout>

    <!-- Side Action Buttons -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="end|center_vertical"
        android:orientation="vertical"
        android:padding="16dp">

        <ImageButton
            android:id="@+id/btnLike"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:background="@drawable/circle_button_bg"
            android:src="@drawable/ic_heart"
            android:contentDescription="Like"
            android:scaleType="centerInside"
            android:padding="14dp" />

        <ImageButton
            android:id="@+id/btnShare"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:layout_marginTop="16dp"
            android:background="@drawable/circle_button_bg"
            android:src="@drawable/ic_share"
            android:contentDescription="Share"
            android:scaleType="centerInside"
            android:padding="14dp" />

    </LinearLayout>

</FrameLayout>